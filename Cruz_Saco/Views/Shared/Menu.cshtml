@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{

}

@*
<div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
    <ul class="navbar-nav flex-grow-1" id="ulOpciones">

        @foreach (var itemPadre in ViewBag.MenuItems)
        {
            if (itemPadre.Cod_Padre == 0)
            {
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        @itemPadre.Nombre
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var itemHijo in ViewBag.MenuItems)
                        {
                            if(itemPadre.Codigo == itemHijo.Cod_Padre)
                            {
                                <a class="dropdown-item" href="@itemHijo.Ruta">@itemHijo.Nombre</a>    
                            }
                        }
                    </div>
                </li>
            }

        }

    </ul>
</div>*@